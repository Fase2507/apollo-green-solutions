# =============================================================================
# Apollo Green Solutions - Project Requirements
# =============================================================================
# This project uses Laravel (PHP) + React/TypeScript (Node).
# Requirements are normally installed through their native manifests:
#   Backend  -> composer.json  (managed with `composer install`)
#   Frontend -> package.json   (managed with `npm install`)
#
# This file is a consolidated, pinned reference of every dependency so you can
# install them all in one place. Pick the section that matches your toolchain.
# =============================================================================

# -----------------------------------------------------------------------------
# 1. SYSTEM PREREQUISITES (install once, outside the project)
# -----------------------------------------------------------------------------
# PHP               >= 8.3
# Composer          >= 2.x
# Node.js           >= 20 (LTS)
# npm               >= 10
# PostgreSQL        >= 16
# Git               latest
# (Docker + docker-compose optional, for containerised dev)

# -----------------------------------------------------------------------------
# 2. BACKEND - PHP extensions required by Laravel 13 (pgsql driver)
# -----------------------------------------------------------------------------
# extension=pdo_pgsql
# extension=pgsql
# extension=mbstring
# extension=openssl
# extension=tokenizer
# extension=xml
# extension=ctype
# extension=json
# extension=bcmath
# extension=fileinfo

# -----------------------------------------------------------------------------
# 3. BACKEND - Composer packages (install with: composer install / require)
# -----------------------------------------------------------------------------
# Laravel 13.x application framework
laravel/framework==13.*
# API authentication (tokens / SPA cookies)
laravel/sanctum==4.3.*
# PostgreSQL driver for Laravel (shipped with framework)
# laravel/serializable-closure (transitive dep, no need to pin)

# --- dev / test ---
# PHPUnit feature/unit tests (ships with a fresh Laravel app)
phpunit/phpunit==^12
# Pest (optional, alternative test runner)
# pestphp/pest==^3

# --- optional ---
# API documentation (auto-generates OpenAPI docs from routes)
# dedoc/scramble==^0.13

# -----------------------------------------------------------------------------
# 4. FRONTEND - npm packages (install with: npm install)
# -----------------------------------------------------------------------------
# React ecosystem
react==^19.2
react-dom==^19.2
typescript==^5.7

# Build tooling
vite==^8.3
@vitejs/plugin-react==^5.0
eslint==^9

# Routing & data fetching
react-router-dom==^7.6
axios==^1.10

# --- optional ---
# Server-state management (caching, auto refetch)
# @tanstack/react-query==^5
# Charts for the energy-monitoring dashboard
# recharts==^3.0
# Styling
# tailwindcss==^4

# -----------------------------------------------------------------------------
# 5. DATABASE
# -----------------------------------------------------------------------------
# Name:     apollo_green_solutions
# Host:     127.0.0.1:5432
# User:     postgres
# Password: (set it in backend/.env as DB_PASSWORD)

# Install PostgreSQL and then create the database:
#   psql -U postgres -c "CREATE DATABASE apollo_green_solutions;"

# -----------------------------------------------------------------------------
# 6. INSTALL CHEAT-SHEET
# -----------------------------------------------------------------------------
#   Backend:
#     composer create-project laravel/laravel backend
#     cd backend
#     composer require laravel/sanctum
#     php artisan install:api
#
#   Frontend:
#     npm create vite@latest frontend -- --template react-ts
#     cd frontend
#     npm install react react-dom react-router-dom axios
#     npm install -D typescript @types/react @types/react-dom