# Frontend environment variables (Vite loads this file)
# VITE_API_URL is the base URL for all API calls from the browser.
# Using "/api" means requests hit the Vite dev server, which proxies
# them to the Laravel backend on http://localhost:8000.
VITE_API_URL=/api